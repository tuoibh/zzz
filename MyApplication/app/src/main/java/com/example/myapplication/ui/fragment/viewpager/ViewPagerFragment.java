/*
package com.example.myapplication.ui.fragment;

import androidx.navigation.fragment.NavHostFragment;

public class ViewPagerFragment extends NavHostFragment {

    @Nullable
    @Override
    public View onCreateView(@NonNull LayoutInflater inflater, @Nullable ViewGroup container, @Nullable Bundle savedInstanceState) {
        View view = super.onCreateView(inflater, container, savedInstanceState);
        ViewPager viewPager = new ViewPager(requireContext());
        viewPager.setId(View.generateViewId());
        ((ViewGroup) view.findViewById(R.id.nav_host_fragment_container)).addView(viewPager);
        setupViewPager(viewPager);
        return view;
    }

    private void setupViewPager(ViewPager viewPager) {
        List<Fragment> fragments = new ArrayList<>();
        fragments.add(new Fragment1());
        fragments.add(new Fragment2());
        fragments.add(new Fragment3());

        MyPagerAdapter adapter = new MyPagerAdapter(getChildFragmentManager(), fragments);
        */
/**//*
viewPager.setAdapter(adapter);
    }
}*/
